<tr>
  <% grid.html_columns(*options[:columns]).each do |column| %>
    <th class="<%= datagrid_column_classes(grid, column) %>">
      <%= column.header %>
      <%= datagrid_order_for(grid, column, options) if column.supports_order? && options[:order]%>
    </th>
  <% end %>
</tr>

<tr class="datagrid-filters">
  <%= fields_for(grid, options) do |f| %>
      <% grid.html_columns(*options[:columns]).each do |column| %>
          <th>
            <%= f.datagrid_filter(column.name) unless grid.filter_by_name(column.name).nil? %>
          </th>
      <% end %>
  <% end %>
</tr>