<%
   # Defining breadcrumbs
   @breadcrumbs = [
           {:link => admin_menu_items_path(:menu_id => @menu_item.menu), :title => t('backend_part.menu_items')},
           {:title => @menu_item.url, :link => '#'},
           {:title => t('backend_part.manage')}
   ]

   # Defining menu
   @menu = [
           {:link => admin_menu_items_path(:menu_id => @menu_item.menu), :title => t('backend_part.manage_menu_items'), :icon => 'list'},
           {:link => new_admin_menu_item_path(:menu_id => @menu_item.menu), :title => t('backend_part.create_menu_item'), :icon => 'file'},
   ]
%>

<div class="page-header">
  <h1><%= t('backend_part.menu_item') %>
    <small>&nbsp;<%= @menu_item.url %></small>
  </h1>
</div>

<div class="form">
    <%= render 'form' %>
</div>