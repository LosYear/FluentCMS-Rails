<%
    # Defining breadcrumbs
    @breadcrumbs = [
        {:link => admin_roles_path, :title => t('backend_part.role')},
        {:title => t('backend_part.create')}
    ]

    # Defining menu
    @menu = [
        {:link => admin_roles_path, :title => t('backend_part.manage_roles'), :icon => 'list'},
    ]
%>

<div class="page-header">
  <h1><%= t('backend_part.role') %>
    <small>&nbsp;<%= t('backend_part.create') %></small>
  </h1>
</div>

<div class="form">
  <%= render 'form' %>
</div>
