<%
    # Defining breadcrumbs
    @breadcrumbs = [
        {:link => admin_menus_path, :title => t('backend_part.menu')},
        {:title => t('backend_part.create')}
    ]

    # Defining menu
    @menu = [
        {:link => admin_menus_path, :title => t('backend_part.manage_menus'), :icon => 'list'},
    ]
%>

<div class="page-header">
  <h1><%= t('backend_part.menu') %>
    <small>&nbsp;<%= t('backend_part.create') %></small>
  </h1>
</div>

<div class="form">
  <%= render 'form' %>
</div>
