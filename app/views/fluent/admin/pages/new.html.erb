<%
    # Defining breadcrumbs
    @breadcrumbs = [
        {:link => admin_pages_path, :title => t('backend_part.page')},
        {:title => t('backend_part.create')}
    ]

    # Defining menu
    @menu = [
        {:link => admin_pages_path, :title => t('backend_part.manage_pages'), :icon => 'list'},
    ]
%>

<div class="page-header">
  <h1><%= t('backend_part.page') %>
    <small>&nbsp;<%= t('backend_part.create') %></small>
  </h1>
</div>

<div class="form">
  <%= render 'form' %>
</div>
