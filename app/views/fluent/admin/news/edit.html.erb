<%
   # Defining breadcrumbs
   @breadcrumbs = [
           {:link => admin_news_index_path, :title => t('backend_part.news')},
           {:title => @news.title, :link => '#'},
           {:title => t('backend_part.manage')}
   ]

   # Defining menu
   @menu = [
           {:link => admin_news_index_path, :title => t('backend_part.manage_news'), :icon => 'list'},
           {:link => new_admin_page_path, :title => t('backend_part.create_news'), :icon => 'file'},
   ]
%>

<div class="page-header">
  <h1><%= t('backend_part.news') %>
    <small>&nbsp;<%= @news.title %></small>
  </h1>
</div>

<div class="form">
    <%= render 'form' %>
</div>