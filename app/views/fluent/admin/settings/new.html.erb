<%
    # Defining breadcrumbs
    @breadcrumbs = [
        {:link => admin_settings_path, :title => t('backend_part.option')},
        {:title => t('backend_part.create')}
    ]

    # Defining menu
    @menu = [
        {:link => admin_settings_path, :title => t('backend_part.manage_settings'), :icon => 'list'},
    ]
%>

<div class="page-header">
  <h1><%= t('backend_part.option') %>
    <small>&nbsp;<%= t('backend_part.create') %></small>
  </h1>
</div>

<div class="form">
  <%= render 'form' %>
</div>
